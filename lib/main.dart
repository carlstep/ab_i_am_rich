import 'package:flutter/material.dart';

// main() is the staring point for all apps followed by runApp(....)
void main() {
  runApp(
    MaterialApp(
        home: Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.green,
        title: Text(
          'I Am Rich',
        ),
      ),
    )),
  );
}
